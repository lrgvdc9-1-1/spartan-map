import { FilterminePipe } from './filtermine.pipe';

describe('FilterminePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterminePipe();
    expect(pipe).toBeTruthy();
  });
});
